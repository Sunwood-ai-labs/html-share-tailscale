# HTML共有くん

HTML共有くんは、AIコーディングエージェントが作ったHTMLレポートをこのPCに保存し、Tailscale経由でスマホから読むためのセルフホスト型ダッシュボードです。AWS、S3、CloudFront、外部データベースは使いません。

## 主な機能

- メモや調査結果を見やすいHTMLに整える
- `html-share page add` でページを登録し、一覧へ反映する
- ローカルJSON状態で既読・スター・レビュー依頼を保存する
- Tailscale ServeのHTTPS URLで一覧とページを共有する
- 有効期限付きのTailnet内共有リンクを発行する
- 作ったHTMLを自分専用の一覧にためる
- Tailnet内限定や期限付きのURLで共有する
- スマホとPCの間で依頼や承認をやり取りする

Tailscaleはアクセス経路です。Tailscale Funnelは使わないため、発行URLもTailnet内限定です。CodexやClaude Codeのスレッドを自動収集する機能ではなく、共有したいHTMLを登録して使います。

## 対応エージェント

中身はCLIとスキルなので、Claude Code、Codex、Cursorなど手元のどのエージェントからでも同じように呼び出せます。

## スマホとの連携

- 思いついた作業は、スマホのインボックスへ置いておける。戻って `/inbox` と頼めばエージェントが引き取る
- 逆向きの `/mobile` は、PCの確認依頼をスマホへ送る。外出先で返した承認やコメントはPC側へ引き継がれる
- 別のペアリングコードは不要で、Tailscaleの接続状態がアクセス境界になります。

> [!NOTE]
> Claude Codeのリモートコントロールとは仕組みが違います。あちらは母艦のPCでセッションを起動したままにしておく必要がありますが、共有くんのやり取りは非同期です。作業をスマホへ送ったあとはPCの電源を切ってよく、外出先で返した回答は、次にPCを開いたときに続きとして取り込めます。

## 頼み方の例

セットアップ後は、エージェントへ日本語で頼めます。

> このHTMLを共有くんに追加して

> このページをTailnet内で7日間共有して

> `/inbox` で、スマホから置いた依頼を引き取って

## セットアップ

初回導入は [初回セットアップ](docs/setup.md) を参照してください。ローカル保存・Tailscale Serve・レビューAPIの構成は [アーキテクチャ](docs/architecture.md)、境界と制約は [セキュリティ設計](docs/threat-model.md) にまとめています。

## ライセンス

Apache License 2.0
